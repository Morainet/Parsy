/**
 * Tailwind CSS v4 uses a PostCSS plugin (no more `tailwind.config.js` by
 * default — configuration is CSS-first via `@theme` in globals.css).
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
